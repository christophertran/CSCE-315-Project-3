<%- include("../partials/header.ejs") %>

    <div class="container favorites-container">
        <div class="row justify-content-center">
            <div class="col-5">
                <h1 class="title-adapt-color" style="padding-bottom: 20px">Favorites</h1>
                <ul id="favorites-list" style="width: 100%">
                    <% currentUser.favorites.forEach(element => { %>
                        <a href="/search?politicianName=<%= element %>">
                            <li class="text-adapt-color">
                                <div class="row" style="padding-bottom: 20px;">
                                    <div class="col-10">
                                        <btn class="btn btn-outline-success" style="font-weight: bold;">
                                            <%= element %>
                                        </btn>
                                    </div>
                                    <div class="col-2">
                                        <% if (currentUser) { %>
                                            <% if (!currentUser.favorites.includes(element)) { %>
                                                <form action="/favorites" method="POST">
                                                    <input type="hidden" id="politicianName" name="politicianName" value="<%= element %>">
                                                    <button class="btn round-button delete-btn"><i class="far fa-trash-alt"></i></button>
                                                </form>
                                            <% } else { %>
                                                <form action="/favorites/?_method=DELETE" method="POST">
                                                    <input type="hidden" id="politicianName" name="politicianName" value="<%= element %>">
                                                    <button class="btn round-button delete-btn"><i class="fas fa-trash-alt"></i></button>
                                                </form>
                                            <% } %>
                                        <% } %>
                                    </div>
                                </div>
                            </li>
                        </a>
                    <% }) %>
                </ul>
            </div>
        </div>
    </div>

<%- include("../partials/footer.ejs") %>